export const environment = {
  production: true,
  baseUrl: 'http://gateway.marvel.com',
  charactersEndpoint: 'v1/public/characters',
  // putting these keys here, we would normally create a backend to keep these in but for the
  // purposes of this demo, keeping them here
  publicKey: '<ADD_PUBLIC_KEY>',
  privateKey: '<ADD_PRIVATE_KEY',
};
